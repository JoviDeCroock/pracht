---
"@pracht/adapter-cloudflare": minor
---

Allow apps and concurrent test runners to select or disable the Cloudflare local-runtime inspector port through `cloudflareAdapter({ inspectorPort })`, and isolate or disable local binding persistence through `cloudflareAdapter({ persistState })`.
