---
"create-pracht": minor
---

Seed five core skills instead of the full 33-skill catalog, and point the generated README and `AGENTS.md` at `pracht skills add` for the rest. Pass `--agent-tools=full` to seed everything as before.
