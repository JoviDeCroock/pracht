---
"@pracht/cli": minor
"create-pracht": minor
---

Rename the authoring MCP server to `pracht dev-mcp`, and register that command in a scaffolded app's `.mcp.json`. `pracht mcp` still works and behaves identically, printing a deprecation notice to stderr.
