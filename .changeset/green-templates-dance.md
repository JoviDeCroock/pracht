---
"@pracht/vite-plugin": patch
"@pracht/preact-ssr-precompile": patch
---

Add the `precompileSsrJsx` opt-in flag to the Pracht Vite plugin and document/benchmark the Preact SSR JSX precompile transform.
