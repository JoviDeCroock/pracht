---
"create-pracht": patch
---

Emit `pnpm-workspace.yaml` only when the package manager is pnpm, and write `tsconfig.json` and `package.json` two-space with conventional key ordering and a trailing newline.
