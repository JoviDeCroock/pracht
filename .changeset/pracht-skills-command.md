---
"@pracht/cli": minor
---

Add `pracht skills list` and `pracht skills add <name...>`, which browse the published skill catalog and install skills into `.claude/skills/`. Each download is checked against the SHA-256 the index publishes.
