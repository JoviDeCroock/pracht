---
"@pracht/vite-plugin": patch
---

Reload the client entry for every route in a multi-file save that adds or removes `loader`, `head`, `headers`, or `getStaticPaths`, instead of only the first one.
