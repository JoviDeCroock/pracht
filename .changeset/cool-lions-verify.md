---
"@pracht/cli": patch
---

Separate verification for bundle budgets, package dependencies, and deployment configuration from manifest, page, and API wiring checks behind the existing verifier facade.
