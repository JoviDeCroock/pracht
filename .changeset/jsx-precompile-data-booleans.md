---
"@pracht/preact-ssr-precompile": patch
---

Serialize boolean `data-*` attributes through the Preact SSR precompile path the same way `preact-render-to-string` does, and expand JSX precompile coverage for upstream transform cases.
