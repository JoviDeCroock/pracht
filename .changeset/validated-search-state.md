---
"@pracht/core": minor
"@pracht/cli": minor
---

Add Standard Schema validation and generated input/output types for route search parameters, plus the `useSearch()` hook. Full hydration revalidates the visitor's current URL without serializing schema output into hydration state.
