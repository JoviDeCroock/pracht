---
"@pracht/core": patch
"@pracht/vite-plugin": patch
---

Include the CSS of the islands a page renders in its document, so island styles no longer arrive after hydration and flash unstyled markup first.
