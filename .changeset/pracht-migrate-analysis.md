---
"@pracht/cli": minor
---

Add `pracht migrate`, which analyses a Next.js App Router project and reports the routes, render modes, and proposed `src/routes.ts` for migrating it to pracht, along with findings graded blocker, review, or note. It reads the project and writes nothing to it.
