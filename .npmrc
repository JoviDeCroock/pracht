# The repo pins Node in package.json#engines, .nvmrc, and .node-version. Without
# this the pin is advisory and an install on an unsupported runtime only fails
# later, in a build step, with an error that names something else.
#
# Published packages carry engines.node too, but advisory there on purpose: a
# consumer chooses their own strictness.
engine-strict=true
