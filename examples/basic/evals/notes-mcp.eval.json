{
  "name": "notes agent flow over MCP",
  "task": "The same notes task an MCP host would run: initialize, search, hit a validation failure as a tool error, then create a note — every step a tools/call against /mcp.",
  "transport": "mcp",
  "steps": [
    {
      "capability": "notes.search",
      "input": { "query": "capabilities" },
      "expect": { "ok": true, "status": 200 }
    },
    {
      "capability": "notes.search",
      "input": { "query": "" },
      "expect": { "ok": false, "errorCode": "invalid_input" }
    },
    {
      "capability": "notes.create",
      "input": { "title": "Created over MCP", "body": "Created by pracht eval over tools/call." },
      "expect": { "ok": true, "output": { "note": { "title": "Created over MCP" } } }
    }
  ]
}
